load data/1a2c.pdb

header = getProperty('fileHeader')
seqres = header.find("SEQRES");

lines = header.split("\n");

//for(i = 1; i < lines.size; i++) {
for(i = 1; i < 3; i++) {
  if(lines[i].find("SEQRES")) {
    print lines[i].split(" ")
  }
}
